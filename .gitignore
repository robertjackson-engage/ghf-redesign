# raw scrape of the original site (large; not needed to build or deploy)
scrape/
# local tooling
.claude/
__pycache__/
*.pyc
.DS_Store
# never commit credentials
.anthropic-key
.env
intellipay/.env
.env

# browser-test scratch output
.playwright-mcp/

# raw tour footage staged at the repo root (the web-ready cuts live in docs/assets/video)
/*.mp4
